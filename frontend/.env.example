# Base URL da API (sem /api no final — o app acrescenta /api).
# Vazio em dev: usa o proxy do Vite → http://localhost:3000
# Frontend local + API na nuvem (reinicie o Vite após salvar):
# VITE_API_URL=https://api.ativadash.com
# Na VPS, em FRONTEND_URL inclua http://localhost:5173 para o browser aceitar CORS.
# VITE_API_URL=
