# Base URL da API. Deixe vazio em dev para usar o proxy (backend em localhost:3000).
# Produção (Pages): use a URL do Worker, ex: https://ativadash-api.xxx.workers.dev
# Dev com Worker local: http://localhost:8787
# VITE_API_URL=
