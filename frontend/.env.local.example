# Copie para frontend/.env.local e reinicie o Vite.
# Modo "só API": nada no PC acessa o Postgres — leitura/escrita só pela API remota.
# Documentação: ../docs/DEV-APENAS-API.md

VITE_API_URL=https://api.ativadash.com

# Opcional: agência filial com menu completo (ADS + Projetos + Equipe).
# VITE_AGENCY_BRANCH_EXPANDED_OPS=1
